# Local state and plan artifacts.
.terraform/
.terraform.lock.hcl.bak
terraform.tfstate
terraform.tfstate.backup
*.tfplan
plan.txt
plan.json
plan_gitlab.json

# Real credentials -- only the .example file is checked in.
terraform.tfvars
